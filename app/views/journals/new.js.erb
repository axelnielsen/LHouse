// Add the dialog title
$('#new_journal_modal h4').html("<i class='fa fa-plus'></i>&nbsp; Registrar Movimiento");

// Render the new form
//$('.modal-body').html('<%= j render("form") %>');

// Show the dynamic dialog
$('#new_journal_modal').modal("show");

// Set focus to the first element
$('#new_journal_modal').on('shown.bs.modal', function () {
	  $('.first_input').focus()
})
